notifications:
  email: false

cache:
  directories:
    - "node_modules"

matrix:
  include:
    - language: node_js
      node_js:
        - "node"

    - language: rust
      install:
        - rustup install nightly
        - npm install --global neon-cli
      script:
        - "neon build"
